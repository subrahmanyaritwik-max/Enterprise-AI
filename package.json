{
  "name": "opspulse-enterprise",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix OPSpulse/FRONTEND",
    "build": "npm run build --prefix OPSpulse/FRONTEND",
    "vercel-build": "npm run build --prefix OPSpulse/FRONTEND"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "zod": "^3.23.8"
  }
}
